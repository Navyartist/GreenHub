subin = ""

print(subin)
subin ="songsubin"
print(subin)
_SUBIN = "kimsubin"
print(_SUBIN)
# variable(변수): var. 데이터를 저장하는 메모리 공간의 이름──안전하지 않음.
# constant(상수): const. 한 번 값이 할당되면 변경할 수 없는 변수──안전함.
# 변하지 않을수록 안전하다
# 파이썬에서는 상수를 대문자로 표기하는 관습만 있고 실제로 상수를 강제하지는 않음 ─> 파이썬 및 자바스크립트가 안정적이지 않은 이유
# 자바스크립트에 const가 있긴 하나 파이썬과 같이 강제하지는 않는다
# 즉 여기서 _대문자: 바꾸지마세요란 암묵의 룰

# 코드를 한글로
# 한글을 코드로
# 의사코드의 도움(+ai의 도움)을 받으려면 코드와 한글을 가깝게 하는게 중요 = 말로 쓰고 코드로 변환하기